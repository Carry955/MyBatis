<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carry.entity.UserMapper">
  <select id="selectUser" resultType="User">
    select * from test where number = #{number}
  </select>
  <!-- 
  通过resultMap实现不同不同类属性名与列名的对应
  <select id="selectUser" resultMap="UserMap">
    select * from test where number = #{number}
  </select>
  <resultMap type="User" id="UserMap">
  	<id column="number" property="number"/>
  	<result column="name" property="name"/>
  	<result column="age" property="age"/>
  	<result column="gender" property="gender"/>
  	<result column="address" property="address"/>
  </resultMap>
   -->
  <select id="selectAll" resultType="User">
  	select * from test
  </select>
  <!-- 使用Map类实现变量注入 -->
  <select id="selectPage" parameterType="Map" resultType="User">
  	select * from test limit #{startIndex}, #{pageSize}
  </select>
  <insert id="addUser" parameterType="User">
  	insert into test(number, name, age, gender) values(#{number}, #{name}, #{age}, #{gender})
  </insert>
  <update id="updateUser" parameterType="User">
  	update test set name=#{name}, age=#{age}, gender=#{gender} where number=#{number}
  </update>
  <delete id="deleteUser">
  	delete from test where number=#{number}
  </delete>
</mapper>